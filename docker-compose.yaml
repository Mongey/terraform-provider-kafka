---
version: "3.2"
services:
  kafka1:
    build:
      context: .
      dockerfile: Dockerfile.kafka
      args:
        broker_id: 1
        listener_host: kafka1
        listener_port: 9092
    ports:
      - "9092:9092"
      - "9093:9093"
